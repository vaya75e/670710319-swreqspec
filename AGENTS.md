# กติกาของโปรเจกต์นี้ (อ่านก่อนทำงานทุกครั้ง)

โปรเจกต์นี้เป็นงานในรายวิชา 520461-165 Software Requirement Specification and Management
เราทำงานแบบ Spec-Driven Development: **spec.md คือความจริงหนึ่งเดียว** โค้ดและแผนงานต้องมาจาก spec เท่านั้น

## ไฟล์สำคัญ

- `specs/NNN-<feature>/spec.md` = ข้อกำหนดของฟีเจอร์ 1 ฟีเจอร์ (คนเขียน คนตรวจ)
- `specs/NNN-<feature>/plan.md` = แผนทางเทคนิคของฟีเจอร์นั้น (AI ร่าง คนตรวจ)
- `docs/srs/` = SRS ฉบับเต็มและ diagram สำหรับคนอ่าน ใช้อ้างอิงเมื่อ spec.md ไม่ชัด
- `prompt-log.md` = บันทึกทุกคำสั่งที่ใช้กับ AI คำถามที่ AI ถาม และคำตอบของทีม **เพิ่มต่อท้ายเท่านั้น ห้ามแก้หรือลบบันทึกเดิม** ถ้าทีมสั่งแก้งานรอบใหม่ ให้เพิ่มบันทึกใหม่ว่าแก้อะไรเพราะอะไร

## กฎ 7 ข้อ

1. ก่อนทำอะไรก็ตาม ให้อ่าน `specs/*/spec.md` ของฟีเจอร์ที่เกี่ยวข้องก่อนเสมอ
2. **ห้ามเดา** ถ้า spec กำกวมหรือขาดข้อมูล ให้ถามกลับ หรือเขียนไว้ในหัวข้อ Open Questions ห้ามตัดสินใจแทนทีม
3. **ห้ามเพิ่มความต้องการ** ที่ไม่มีใน spec แม้จะ "ระบบแบบนี้มักมี" ก็ตาม
4. **ห้ามสร้างสิ่งที่อยู่ใน Out of scope**
5. **ห้ามเปลี่ยนหรือสร้าง ID ใหม่** ทุก FR / NFR / CON / IF / DOM / AC ต้องใช้ ID เดิมจาก spec ถ้าจำเป็นต้องมีข้อใหม่ ให้เสนอเป็นคำถาม ไม่ใช่เขียนลงไปเอง
6. ระบุเทคโนโลยี (ภาษา ฐานข้อมูล framework) ได้เฉพาะที่อยู่ในหัวข้อ Constraints ถ้า spec ไม่ระบุ ให้เขียนกำกับว่า "ทีมเลือกเอง ไม่ได้มาจาก spec"
7. ทุกอย่างที่สร้างต้องอ้างกลับไปหา ID ใน spec ได้ เช่น ฟังก์ชันระบุว่ารองรับ FR ข้อใด test ตั้งชื่อตาม AC ID

## ภาษา

ตอบและเขียนเอกสารเป็นภาษาไทย ยกเว้นชื่อไฟล์ โค้ด คำสั่ง และ ID
